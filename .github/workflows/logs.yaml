name: Show some logs
run-name: Informative logs

on: 
  pull_request:
    types: [ labeled ]

jobs:
  CoolJobName:
    runs-on: ubuntu-latest

    steps:
      - name: first log
        run: echo "Hello world!"
